@page "/auth/redirecttologin"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // return url
        Navigation.NavigateTo($"/auth/login/{Uri.EscapeDataString(Navigation.Uri)}");
    }
}
